apiVersion: v1
kind: ConfigMap
data:
  config.json: |-
    {
        "auths": {
            "https://index.docker.io/v1/": {
                "auth": "{{ (docker_hub.username + ':' + docker_hub.password) | b64encode }}"
            }
        }
    }
metadata:
  name: kaniko-dockerhub
